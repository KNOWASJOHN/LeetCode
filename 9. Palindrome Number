class Solution(object):
    def isPalindrome(self, x):
        n = str(x)
        s = len(n)
        if (x<0):
            return False
        if(n!=n[::-1]):
            return False
        else:
            return True
